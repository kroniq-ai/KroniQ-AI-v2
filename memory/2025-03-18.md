# 2025-03-18

## VØYD MVP scaffold completed

- **Scope:** Full MVP per spec: idea → startup launch package with CEO assistant
- **Implemented:**
  - DB schema (Supabase): projects, jobs, agents, research_items, competitors, mvp_features, marketing_assets, financials, memories, artifacts, api_keys + RLS
  - Model router (`lib/model-router.ts`) — task-based OpenRouter model selection
  - Orchestrator (`lib/orchestrator.ts`) — CEO → Research → Product → CTO → CMO → CFO sequential flow
  - Agent prompts (CEO, Research, Product, CTO, CMO, CFO) in `lib/agents/prompts.ts`
  - API: POST/GET projects, GET project/:id, POST project/:id/run, GET jobs/:id, auth (login/signup/logout/callback)
  - UI: Dashboard, Project workspace (tabs: Research, Product, Tech, Marketing, Finance, Artifacts), CEO Assistant widget, IdeaInput modal
  - Auth: Supabase (email + Google OAuth), middleware for session refresh
- **Dependencies added:** @supabase/supabase-js, @supabase/ssr, openai (for OpenRouter)
- **Fixed:** Pre-existing lint errors in Footer, FinalCTA, UseCases, VisionSection, SmoothScroll, WaitlistModal
- **Note:** Build completes; final trace step may fail on Windows with path containing Ø — known Next.js quirk

## MVP enhancements (later same day)

- **Layout:** AppSidebar with Research/Product/Tech/Marketing/Finance/Artifacts tabs; DockNav hidden on app routes (fixed hooks order)
- **Agent chat:** Each tab has AgentChat component — prompt input to run that agent via POST /api/projects/:id/agent
- **Tech:** Website preview iframe in TechCard; Copy + Download HTML
- **Marketing:** SocialPostCard + EmailTemplateCard with copy buttons; hashtag_suggestions stored in metadata
- **Product:** Logo section with Generate Logo button (kie.ai placeholder; user will add API)
- **CEO Assistant:** Rebuilt UI — gradient FAB, Sparkles icon, chat bubbles, quick actions, Loader when running
- **Models:** Already using free models (gemini-2.0-flash-exp, llama-3.2-3b-instruct) via model-router
