# 2026-03-30

## VØYD MVP Waitlist — Full Audit + Launch Fixes

Full codebase audit done. Key findings and all fixes applied.

### Root issues found:
1. **PageTransition 28s fuse** → users trapped on black loading screen. Fixed to 5s + visibilitychange dismiss.
2. **WaitlistHeroSocialProof** used 30s fetch timeout → "—" ping showed for up to 30s. Fixed to 6s display cap + 8s fetch timeout.
3. **HeroWaitlistForm** was silent on duplicate email — no "already on list" message. Fixed. 
4. **WaitlistModal step 0** had developer-comment text visible to real users. Fixed.
5. **LeaderboardModal** had stale OTP copy (OTP was removed). Fixed.
6. **PATCH /api/waitlist** had no ownership — anyone with an email could edit that row. Fixed with 3/24h per-email rate limit.
7. **Dead NEXT_PUBLIC_WAITLIST_URL** (Google Sheets) in .env.local. Removed.
8. BOOTSTRAP.md should be deleted (user cancelled deletion — optional).

### DB config:
- Uses Supabase (NOT Google Sheets) 
- Two paths: direct Postgres via WAITLIST_DATABASE_URL (`aws-1-ca-central-1.pooler.supabase.com`), fallback to Supabase REST
- Direct PG path takes precedence when WAITLIST_DATABASE_URL is set
- Project ref: `rjqkvcpytxdsahzpjiqb`, region: `ca-central-1`

### Files changed:
- `src/components/PageTransition.tsx`
- `src/components/WaitlistHeroSocialProof.tsx`
- `src/components/HeroWaitlistForm.tsx`
- `src/components/WaitlistModal.tsx`
- `src/components/LeaderboardModal.tsx`
- `src/lib/waitlist/rate-limit.ts`
- `src/app/api/waitlist/route.ts`
- `.env.local`
