- Referral URLs: `src/lib/waitlist/public-site-url.ts` (`resolvePublicSiteOriginServer`, default voyd.one); `GET /api/public-config`; `publicSiteUrl` on waitlist + complete-oauth responses; `PublicSiteConfigLoader` + `setPublicSiteOriginOverride` / `subscribePublicSiteOrigin` in `client-session.ts`; Leaderboard, Hero form, Waitlist modal use `useSyncExternalStore` for share links. `.env.local` includes `NEXT_PUBLIC_SITE_URL`. README deploy checklist added.

- Canonical domain defaults updated to **voyd.one** (layout, robots, sitemap, demo not-found, `.env.example`, waitlist comment). Contact mailto set to **hello@voyd.one** (Footer, terms, privacy).

- `next build`: fixed prerender “Functions cannot be passed to Client Components” by setting `export const dynamic = "force-dynamic"` on root `src/app/layout.tsx` (PageTransition + RSC). Removed redundant per-route layouts (login, signup, demo, cto, dashboard).
- `/cto`: `CTOPageClient.tsx` wraps sections + Footer in one client tree (mirrors home pattern); `page.tsx` only exports metadata + `<CTOPageClient />`.

- Landing progressive blur: shorter band (`min(26vh,240px)`), 4 `backdrop-filter` layers + gradient (was 7), masks biased low; `contain: paint` on overlay. Perf: intro `INTRO_MIN_MS` 3200; `ShaderAnimation` maxDpr 1, renderScale 0.65, fpsCap 18 (PageTransition + RouteLoadingView); hero `ThermodynamicGrid` resolution 16; Lenis duration 1, touchMultiplier 1.25.

- Waitlist: removed email OTP for signup (`POST /api/waitlist` no session gate; `WaitlistModal` + `HeroWaitlistForm` submit without Supabase verify). `member-stats` allows referral-code lookup without login. Copy: top 3 referrers get early access + free Pro at launch (Leaderboard quick rules, FAQ, `ReferralLeaderboardNotice`). **Risk:** `PATCH /api/waitlist` updates profile by email only—anyone who knows an email could change that row; acceptable per “no verification” product choice.
