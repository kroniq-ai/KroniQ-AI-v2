"use client";

import { GlassPipeDivider } from "@/components/ui/glass-pipe-divider";

/** Light zone → dark sticky scroll */
export function LightToDarkBridge() {
  return <GlassPipeDivider variant="light-to-dark" />;
}
