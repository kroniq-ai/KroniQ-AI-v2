"use client";

/**
 * Was a viewport-fixed bottom `backdrop-filter` band for the dock. That stack reads as a milky
 * “fog” / gray haze on top of a true #050607 hero, so the effect is disabled. Re-enable by
 * portaling a blur layer to `document.body` (see git history) if a future layout needs it.
 */
export default function LandingProgressiveBlur() {
    return null;
}
